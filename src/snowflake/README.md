# Snowflake

Live Snowflake querying is implemented in:
- `integrated_system/app/ai_app/snowflake_service.py`

If Snowflake credentials are not set, the system falls back to sample structured facts (still demonstrating the end-to-end pipeline).
