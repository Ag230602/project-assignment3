# Data

This project uses a lightweight JSON knowledge base:
- `integrated_system/app/knowledge_base.json`

If you want an ingestion pipeline, add scripts here that transform raw docs into the JSON format consumed by `integrated_system/app/ai_app/knowledge_base.py`.
